# Example local paths for this project.
# The Python scripts do not load this file automatically.
# Use these values as a reference when passing command-line arguments.

DATA_ROOT=path/to/mvtec_ad
CATEGORY=cable
TRAIN_OUTPUT_DIR=path/to/results

CHECKPOINT_PATH=path/to/model.ckpt
INPUT_PATH=path/to/input_image_or_folder
INFERENCE_OUTPUT_DIR=path/to/output_dir
THRESHOLD=0.5
